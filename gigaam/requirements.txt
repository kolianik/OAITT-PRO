# Docker image installs via staged install_deps.sh (avoids pip resolution-too-deep).
# For local dev, install in the same order:
-r requirements-web.txt
-r requirements-onnx.txt
# torchcodec + nvidia-npp-cu12: use pytorch cu128 index with constraints.txt
-r requirements-audio.txt
pyannote.audio==4.0.4
-r requirements-ml.txt
